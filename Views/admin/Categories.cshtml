@model List<E_Commerce_Cooperatives.Models.ECommerceDbContext.CategorieStats>

@{
    ViewBag.Title = "Gestion des Catégories";
    Layout = "~/Views/Admin/_AdminLayout.cshtml";
    
    var totalCategories = Model.Count;
}

<div class="admin-page">
    <!-- Top Header with Search -->
    <div class="admin-page-header-row">
        <form action="@Url.Action("Categories")" method="get" class="search-form">
            <div class="search-input-wrapper">
                <i class="fas fa-search search-icon"></i>
                <input type="text" name="searchTerm" value="@ViewBag.SearchTerm" placeholder="Rechercher..." class="search-input-field" />
            </div>
        </form>
        <div class="header-notifications">
           <!-- Placeholder for visual consistency with image -->
        </div>
    </div>

    <!-- Page Title Section -->
    <div class="page-title-section">
        <h1 class="main-title">Gestion des Catégories</h1>
        <p class="sub-title">Organisez vos produits par catégories</p>
    </div>

    <!-- Controls Row -->
    <div class="categories-controls">
        <p class="categories-count">@totalCategories catégories au total</p>
        <a href="@Url.Action("AjouterCategorie")" class="btn-primary-action">
            <i class="fas fa-plus"></i> Nouvelle catégorie
        </a>
    </div>

    <!-- Grid -->
    <div class="categories-grid">
        @foreach (var cat in Model)
        {
            <div class="category-card">
                <!-- Header Image/Icon -->
                <div class="category-card-header">
                    @if (!string.IsNullOrEmpty(cat.ImageUrl))
                    {
                        <img src="@Url.Content(cat.ImageUrl)" alt="@cat.Nom" class="category-image-real" />
                    }
                    else
                    {
                        <div class="category-folder-icon">
                            <i class="far fa-folder-open"></i>
                        </div>
                    }

                    <!-- Actions Floating in Header (Bottom Right) -->
                    <div class="card-actions">
                        <a href="@Url.Action("ModifierCategorie", new { id = cat.CategorieId })" class="btn-action-icon btn-edit-icon" title="Modifier">
                            <i class="fas fa-pen"></i>
                        </a>
                        <button onclick="toggleStatus(@cat.CategorieId)" class="btn-action-icon btn-delete-icon" title="@(cat.EstActive ? "Désactiver" : "Activer")">
                            <i class="fas fa-trash"></i>
                        </button>
                    </div>
                </div>

                <!-- Body -->
                <div class="category-card-body">
                    <div class="card-title-row">
                        <h3 class="category-title">@cat.Nom</h3>
                        <span class="status-badge @(cat.EstActive ? "active" : "inactive")">
                            @(cat.EstActive ? "Active" : "Inactive")
                        </span>
                    </div>
                    
                    <p class="category-desc">
                        @(string.IsNullOrEmpty(cat.Description) ? "Aucune description" : (cat.Description.Length > 60 ? cat.Description.Substring(0, 60) + "..." : cat.Description))
                    </p>
                    
                    <div class="category-footer">
                        <span class="product-count">@cat.NombreProduits produits</span>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

@section scripts {
<script>
     // ca pour le filtrage automatique sans cmliquer entre 
      $(document).ready(function() {
        var allCards = $('.category-card').toArray();
        var totalCategories = allCards.length;

        $('#searchInput').on('keyup', function() {
            var searchTerm = $(this).val().toLowerCase().trim();
            
            if (searchTerm === '') {
                // Afficher toutes les cartes
                $(allCards).show();
                $('#noResultsMessage').hide();
                $('#displayedCount').text(totalCategories);
                return;
            }

            // Filtrer les cartes
            var visibleCount = 0;
            
            $(allCards).each(function() {
                var $card = $(this);
                var name = $card.data('name') || '';
                var description = $card.data('description') || '';
                
                // Vérifier si le terme de recherche est dans le nom ou la description
                if (name.includes(searchTerm) || description.includes(searchTerm)) {
                    $card.show();
                    visibleCount++;
                } else {
                    $card.hide();
                }
            });

            // Mettre à jour le compteur
            $('#displayedCount').text(visibleCount);

            // Afficher/masquer le message "aucun résultat"
            if (visibleCount === 0) {
                $('#noResultsMessage').show();
            } else {
                $('#noResultsMessage').hide();
            }
        });
    });

    function toggleStatus(id) {
        if (confirm("Voulez-vous vraiment changer le statut de cette catégorie ?")) {
            fetch('@Url.Action("ToggleCategorieStatus", "Admin")', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ id: id })
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    location.reload();
                } else {
                    alert("Erreur lors de la mise à jour du statut.");
                }
            });
        }
    }
</script>
}

<style>
    /* Global Clean Layout additions */
    .admin-page-header-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 30px;
    }

    .search-input-wrapper {
        position: relative;
        width: 400px;
    }

    .search-icon {
        position: absolute;
        left: 15px;
        top: 50%;
        transform: translateY(-50%);
        color: #95A5A6;
    }

    .search-input-field {
        width: 100%;
        padding: 12px 12px 12px 40px;
        border-radius: 8px; /* Slightly rounded as per image */
        border: 1px solid #D09E86; /* The brownish border color from image */
        background: white;
        font-size: 14px;
        outline: none;
        color: #555;
    }

    .page-title-section { margin-bottom: 25px; }
    .main-title {
        font-family: 'Georgia', serif; /* Serif font as in image */
        color: #2D4C61; /* Dark blue/teal */
        font-size: 28px;
        font-weight: 700;
        margin-bottom: 5px;
    }
    .sub-title { color: #7F8C8D; font-size: 14px; margin: 0; }

    .categories-controls {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
    }

    .btn-primary-action {
        background-color: #2D4C61; /* Matches button in image */
        color: white;
        padding: 10px 20px;
        border-radius: 8px;
        text-decoration: none;
        font-weight: 600;
        font-size: 14px;
        display: inline-flex;
        align-items: center;
        gap: 8px;
    }
    .btn-primary-action:hover { background-color: #1a3344; color: white; }

    /* Grid & Cards */
    .categories-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
        gap: 24px;
    }

    .category-card {
        background: #FDFBF7; /* Very light beige/white bg */
        border-radius: 12px;
        overflow: hidden;
        border: none;
        box-shadow: 0 4px 15px rgba(0,0,0,0.03);
        display: flex;
        flex-direction: column;
    }

    .category-card-header {
        height: 160px;
        background-color: #F1EBE4; /* darker beige header */
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
    }

    .category-folder-icon { font-size: 50px; color: #A0A0A0; }
    .category-image-real { width: 100%; height: 100%; object-fit: cover; }

    /* Action Buttons - Bottom Right of Header */
    .card-actions {
        position: absolute;
        bottom: 15px;
        right: 15px;
        display: flex;
        gap: 10px;
    }

    .btn-action-icon {
        width: 32px;
        height: 32px;
        border-radius: 8px; /* Squared with rounded corners */
        display: flex;
        align-items: center;
        justify-content: center;
        border: none;
        cursor: pointer;
        transition: all 0.2s;
        text-decoration: none;
        font-size: 14px;
    }

    /* Edit: Beige/Yellowish bg, dark icon */
    .btn-edit-icon {
        background-color: #EADBC0; 
        color: #2C3E50;
    }
    .btn-edit-icon:hover { background-color: #dcc8a4; }

    /* Delete: Red bg, white icon */
    .btn-delete-icon {
        background-color: #EB5757;
        color: white;
    }
    .btn-delete-icon:hover { background-color: #c0392b; }

    /* Body */
    .category-card-body { padding: 20px; flex: 1; display: flex; flex-direction: column; }
    
    .card-title-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 10px;
    }

    .category-title {
        font-family: 'Georgia', serif;
        color: #2D4C61;
        font-size: 18px;
        margin: 0;
        font-weight: 700;
    }

    .status-badge {
        font-size: 10px;
        padding: 3px 10px;
        border-radius: 12px;
        font-weight: 700;
        text-transform: uppercase;
    }
    .status-badge.active { background-color: #6B8E23; color: white; }
    .status-badge.inactive { background-color: #95A5A6; color: white; }

    .category-desc {
        font-size: 13px;
        color: #5D6D7E;
        margin-bottom: 15px;
        line-height: 1.4;
        flex: 1;
    }

    .category-footer {
        color: #2D4C61;
        font-weight: 600;
        font-size: 13px;
    }
</style>
